type User {
  id: ID! @unique
  name: String!
}

type Test {
  id: ID! @unique
  name: String!
}

type Item {
  id: ID! @unique
  title: String!
  description: String!
  image: String
  largeImage: String
  price: Int!
}